# The sqlmesh data volume. It currently holds only the log the entrypoint tees here (so a
# crash leaves a log on disk), but it is a general data volume for the service. It
# survives stopping the stack; remove it explicitly to discard the data.
[Volume]
VolumeName=sqlmesh_data
