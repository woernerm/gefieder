# The sftp data volume, holding the SSH host key the endpoint generates on its first start:
# a changed one would make SFTP clients refuse to reconnect. It survives stopping the
# stack; remove it to discard the data.
[Volume]
VolumeName=sftp_data
