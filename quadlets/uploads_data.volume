# The uploads volume holding the files uploaded through dropzones. crudman writes it;
# the sqlmesh container mounts it read-only at the same path, so the file paths stored
# in the crudman.dropzones_upload rows resolve in both containers. It survives stopping
# the stack; remove it explicitly to discard the data.
[Volume]
VolumeName=uploads_data
