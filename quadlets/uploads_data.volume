# The files uploaded through dropzones. crudman writes it and sqlmesh mounts it read-only
# at the same path, so the paths in crudman.dropzones_upload resolve in both. It survives
# stopping the stack; remove it to discard the data.
[Volume]
VolumeName=uploads_data
