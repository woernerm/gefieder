# The crudman data volume. It currently holds only the logs the entrypoints tee here (so
# a crash leaves a log on disk): crudman.log from the admin panel and sftp.log from the
# sftp container, which runs the same application. It survives stopping the stack;
# remove it explicitly to discard the data.
[Volume]
VolumeName=crudman_data
