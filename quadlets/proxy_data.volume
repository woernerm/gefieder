# The proxy data volume. It currently holds only visits.log, the structured page-visit log
# nginx writes for the server-statistics collector to drain, but it is a general data volume
# for the service. It survives stopping the stack; remove it explicitly to discard the data.
[Volume]
VolumeName=proxy_data
