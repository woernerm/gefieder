# The proxy data volume, currently holding only visits.log, the page-visit log nginx writes
# for the collector to drain. It survives stopping the stack; remove it to discard the
# data.
[Volume]
VolumeName=proxy_data
