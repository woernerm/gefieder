[project]
name = "crudman"
version = "0.1.0"
description = "Crudman is an administration panel for the analytics platform."
requires-python = ">=3.13"
dependencies = [
    "asyncssh",
    "Django",
    "django-unfold",
    "gunicorn",
    "polars",
    "psycopg[binary]",
    "whitenoise",
]
