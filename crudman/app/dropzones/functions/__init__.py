"""The designated folder for dropzone check and convert functions.

Every module placed here is imported at startup (see ``dropzones.registry``), so a
function only needs the ``@checker``/``@converter`` decorator to become selectable on a
dropzone in the admin.
"""
